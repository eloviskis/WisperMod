# WisperMod — dependências para o executável desktop
#
# IMPORTANTE: instale PyTorch com CPU-only ANTES deste arquivo
# para manter o pacote menor (~300 MB vs ~2 GB com CUDA):
#
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# Depois:
#   pip install -r requirements_desktop.txt

customtkinter>=5.2.0
openai-whisper>=20231117
pyinstaller>=6.3.0
